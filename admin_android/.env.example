# Sideload signing for the Lonora admin WebView APK.
# Copy values into keystore/keystore.properties — do not commit the real file.
ANDROID_KEYSTORE_FILE=keystore/lonora-admin.jks
ANDROID_KEYSTORE_ALIAS=lonora-admin
ANDROID_KEYSTORE_PASSWORD=change-me
ANDROID_KEY_PASSWORD=change-me
