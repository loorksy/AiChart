import { ConsolePageSkeleton } from "@/components/ui/skeletons/page-skeletons";

/** Recommendation cards. */
export default function ConsoleRecommendationsLoading() {
  return <ConsolePageSkeleton shape="cards" maxWidth="max-w-5xl" />;
}
