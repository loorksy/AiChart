import { PerformanceJournalClient } from "@/components/recommendations/PerformanceJournalClient";

/** Performance Journal (Group 9) — descriptive only, never a gate. */
export default function JournalPage() {
  return <PerformanceJournalClient />;
}
