import { QuantAgentFeedClient } from "@/components/quantAgent/QuantAgentFeedClient";

/** Quant Agent — a second, independent recommendation engine (plan §4). */
export default function QuantAgentPage() {
  return <QuantAgentFeedClient />;
}
