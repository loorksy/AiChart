.cursor/mcp.json
graphify-out/
infra/crypto-endpoints.local.env

# Node
node_modules/
dist/
.next/
*.tsbuildinfo

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/

# Environment / secrets — never commit real env files
.env
.env.*
!.env.example
!*.env.example

# Local clone scratch for upstream packages (vendored copies live under web/vendor/)
.tmp-realtime-voice-component/

# Local databases and runtime state
data/
*.db
*.sqlite
*.sqlite3

# Test output / coverage
coverage/
.coverage
test-results/

# Build/deploy archives and dumps — deploy from git, not tarballs
*.tgz
*.tar.gz
*.tar
*.b64
*.log

# Editor / OS
.DS_Store
*.swp
.gstack/

# --- merged from web/.gitignore (Next.js app) ---
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local data (sqlite)
/data

/testsprite_tests/

# TradingView Advanced Charting Library (proprietary - never commit)

# TradingView Advanced Charts is committed here deliberately (2026-08-01).
# It is LICENSED, not open source: this repository is PRIVATE for that reason
# and must stay private. Making it public would redistribute the library.
dump.rdb
