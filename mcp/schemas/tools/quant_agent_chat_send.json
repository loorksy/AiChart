{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "chatId": {
      "description": "Existing Quant Agent chat session id; omit to start a new session",
      "type": "string",
      "minLength": 1
    },
    "message": {
      "type": "string",
      "minLength": 1,
      "description": "User message to send to the Quant Agent chat assistant"
    }
  },
  "required": [
    "message"
  ]
}
