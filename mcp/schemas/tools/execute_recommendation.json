{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "recommendation_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "description": "The recommendation to execute (its id)."
    },
    "volume": {
      "type": "number",
      "exclusiveMinimum": 0,
      "maximum": 1000,
      "description": "Lots, as the operator answered. The server re-validates against the broker's grid and margin."
    },
    "idempotency_key": {
      "type": "string",
      "minLength": 8,
      "maxLength": 80,
      "description": "One key per operator request. Reuse it on retries of the SAME request so a double send stays one order."
    }
  },
  "required": [
    "recommendation_id",
    "volume",
    "idempotency_key"
  ]
}
