# برومبت Claude Code — بيانات من حساب MT فقط + واجهة ربط احترافية + باقة واحدة

> انسخ هذا الملف كاملاً إلى محادثة Claude Code التي تعمل على مستودع AiChart.
> ابدأ من `main` المحدّث، وأنشئ فرعاً جديداً لهذا العمل. لا تخلط هذا التغيير مع فروع أخرى غير مكتملة.

---

أنت مهندس TypeScript كبير تعمل في مستودع **AiChart / Lonora** (`github.com/loorksy/AiChart`، خاص). المنصة RTL-first: Next.js 16 App Router، React 19، TypeScript، Tailwind v4، وخادم MCP منفصل تحت `mcp/`.

## الهدف بدقة (ثلاثة محاور غير قابلة للتفاوض)

1. **إلغاء مزود بيانات OANDA بالكامل.** لا تغذية سوق للمنصة بدون حساب MetaTrader مرتبط. كل مستخدم يُطلب منه أولاً ربط حساب MT، والبيانات (شموع، أسعار، رموز، تيكات) تأتي من حساب المستخدم نفسه عبر MetaApi فقط.
2. **إعادة تصميم تجربة ربط MT لتبدو كمنصة MetaTrader الاحترافية** — أيقونة MT5 الرسمية → نافذة منبثقة → بحث وسيط لحظي مع شعار → ثم دخول (رقم الحساب / كلمة المرور / الخادم).
3. **باقة اشتراك واحدة + تجربة مجانية واحدة:**
   - **التجربة المجانية:** كل المميزات، لمدة **ساعة واحدة فقط**، وبحد أقصى **3 توصيات**.
   - **المدفوع:** كل شيء، السعر **180$**.

لا تبنِ منتجاً جديداً بجانب الموجود. عدّل المسارات والواجهات والعقود الحالية حتى تطابق هذا النموذج.

---

## ما هو مبني مسبقاً — لا تعِد بناءه

اقرأ هذه الملفات قبل أي تخطيط أو كود:

| القدرة | أين | الحالة |
|---|---|---|
| معالج ربط MT5 | `web/src/components/mt5/Mt5ConnectWizard.tsx` | يعمل: بحث خادم + بيانات دخول + ربط عبر `/api/mt/connect` |
| صفحة الربط | `web/src/app/console/mt5/page.tsx` | موجودة |
| بطاقة الإعدادات | `web/src/components/settings/Mt5LinkCard.tsx` | رابط إلى `/console/mt5` — بلا نافذة منبثقة، بلا أيقونة MT5 الرسمية، بلا زر فك ربط مباشر |
| بحث الوسطاء | `web/src/app/api/mt5/brokers/route.ts` | يوكّل MetaApi provisioning؛ يعيد أسماء خوادم فقط حالياً |
| تدفق الربط/الفصل | `web/src/lib/mtConnectFlow.ts` (`connectMtAccount` / `disconnectMtAccount`) | موجود ويعمل |
| حل مصدر البيانات | `web/src/lib/markets/marketDataSource.ts` | اليوم: `oanda` \| `metaapi` مع سقوط افتراضي إلى OANDA |
| عميل OANDA | `web/src/lib/markets/oanda.ts` + مسارات klines/ticks/instruments/forex-price | ما زال أنبوب المنصة الافتراضي |
| باقة Lonora الكنسية | `web/src/lib/subscription/plan.ts` | `promotionalPriceUsd: 180` و `trialInteractions: 3` موجودة — **لا يوجد حد زمني ساعة واحدة بعد** |
| حصة التجربة | `web/src/lib/subscription/trialQuota.ts` + `entitlement.ts` | تحصي التفاعلات فقط؛ لا تنتهي التجربة بعد 60 دقيقة |
| صفحة الأسعار العامة | `web/src/app/pricing/page.tsx` + `web/src/lib/billing/tiers.ts` | ما زالت تعرض **أربع باقات** (lite/plus/pro/promax) — تتعارض مع باقة واحدة |

**لا تثبّت حزمة موجودة مسبقاً. لا تنشئ نظام widgets أو billing ثانٍ بجانب الموجود.**

---

# الجزء 1 — إلغاء OANDA وجعل MT شرطاً للبيانات

## القاعدة الجديدة

```
لا حساب MT مرتبط → لا بيانات سوق (شموع / أسعار / رموز / تيكات).
حساب MT مرتبط عبر MetaApi → كل البيانات من حساب المستخدم فقط.
لا يوجد أنبوب "منصة" بديل. لا fallback إلى OANDA.
```

## المطلوب

1. **احذف أو اعزل مسار OANDA كمصدر بيانات حي للمستخدمين:**
   - `web/src/lib/markets/oanda.ts` ومستهلكوه في:
     - `web/src/app/api/market/klines/route.ts`
     - `web/src/app/api/market/ticks/route.ts`
     - `web/src/app/api/market/forex-price/route.ts`
     - `web/src/app/api/instruments/route.ts`
     - `web/src/app/api/instruments/quotes/route.ts`
     - أي مسار agent/MCP يعتمد OANDA كـ fallback
   - `resolveMarketDataSource` يجب ألا يعيد `"oanda"` أبداً بعد هذا التغيير.
   - حدّث النوع `MarketDataSource` ليصبح مصدر الوسيط فقط (مثلاً `"metaapi"` فقط، أو أعد تسميته بوضوح). لا تترك `"oanda"` كقيمة حية في الواجهة أو الـ API.

2. **بوابة إلزامية لربط MT قبل استخدام الشارت/الوكيل:**
   - مستخدم بلا `metaapi_account_id` صالح → واجهة واضحة تطلب الربط (أيقونة MT5 + CTA)، وليس شموعاً من مزود آخر.
   - مسارات السوق ترجع خطأ منظّم وواضح (مثل `mt_not_linked`) بدل إجابة صامتة أو بيانات مزوّد منصة.
   - حدّث نصوص الخصوصية/الإدارة/المفاتيح التي ما زالت تذكر OANDA كمصدر بيانات (`privacy/page.tsx`، `AdminKeysPanel.tsx`، …).

3. **Warehouse / cron المعتمد على OANDA:**
   - إن وُجدت مهام تعبئة تاريخية من OANDA (`api/cron/candle-warehouse` وما شابه)، إمّا أوقفها كمصدر منصة، أو أعد توجيهها لتاريخ حساب المستخدم فقط. لا تترك مساراً يملأ بيانات عامة من OANDA ويعرضها للمستخدمين.
   - لا تكسر ترحيل المخططات؛ غيّر السلوك لا الجداول بلا داعٍ.

4. **الاختبارات والعقود:**
   - حدّث كل اختبار يشير إلى `"oanda"` كمصدر افتراضي (مثل `smartChartWorkspaceInit.test.ts`).
   - حدّث وصف MCP / tool inventory إن ذكر أنبوبين `oanda` + `metaapi`.
   - شغّل `cd web && npm run test:ci` و`npx tsc --noEmit` (تجاهل أخطاء vendor TradingView المعروفة مسبقاً).

### معيار القبول للجزء 1

- [ ] لا يوجد مسار مستخدم يعيد شموع/أسعار من OANDA.
- [ ] مستخدم بلا حساب MT لا يرى بيانات سوق حية؛ يُوجَّه للربط.
- [ ] مستخدم مربوط يرى رموز/شموع وسيطه فقط (تهجئة الوسيط حسّاسة لحالة الأحرف — لا تُحوَّل إلى uppercase عشوائياً).
- [ ] لا بقايا UI لاختيار "OANDA" كمصدر بيانات.

---

# الجزء 2 — واجهة ربط MT احترافية (تجربة شبيهة بـ MT5)

## النية التصميمية

المستخدم يجب أن يشعر أنه داخل تطبيق MetaTrader، لا داخل نموذج إعدادات عام:

1. يظهر **زر/أيقونة MT5 الرسمية** (في الإعدادات / الشارت / حالة الاتصال).
2. عند الضغط تفتح **نافذة منبثقة (modal)** — ليس انتقال صفحة كامل كالمسار الوحيد.
3. داخل النافذة تدفق من خطوتين (أو أكثر إن لزم MetaApi):

### الخطوة أ — البحث عن الوسيط
- خانة بحث لحظي: «ابحث عن وسيطك» (مثل تطبيق MT5).
- نتائج فورية أثناء الكتابة.
- **كل نتيجة تعرض شعار الوسيط** بجانب الاسم (مزامنة شعار الوسيط إلزامية).
- عند اختيار وسيط → انتقال تلقائي للخطوة ب مع قائمة خوادم ذلك الوسيط.

### الخطوة ب — تسجيل الدخول
أربع خانات واضحة (أو ما يعادلها بصرياً):
1. الوسيط المختار (للعرض / قابل للتغيير بالرجوع)
2. **رقم الحساب (Login)**
3. **كلمة المرور**
4. **الخادم** — قائمة بجميع خوادم الوسيط المختار (ليست كتابة حرة كالمسار الوحيد؛ الإدخال اليدوي يبقى احتياطياً فقط إن فشل البحث)

ثم زر ربط، مع شفافية الأمان الحالية (تشفير AES-256، MetaApi مرخّص) دون إخافتها بتحذيرات مكررة.

### إدارة الحساب المرتبط
- إن وُجد حساب مرتبط: عرض الوسيط + الشعار + رقم الحساب + الخادم + الحالة.
- زر **«إلغاء الربط»** يستدعي `disconnectMtAccount` (عبر `/api/mt/connect` DELETE أو المسار القائم).
- بعد الإلغاء يمكن **ربط حساب جديد** من نفس الواجهة فوراً.
- لا تترك المستخدم عالقاً على صفحة `/console/mt5` فقط — الصفحة يمكن أن تبقى كمسار عميق، لكن المدخل الأساسي يصبح الـ modal من الأيقونة.

## MetaApi أولاً

- تحقق من وثائق/إمكانيات MetaApi (Provisioning / UI) هل توفر واجهة ربط جاهزة أو حقول شعارات الوسطاء.
- **إن وفّرت MetaApi واجهة/بيانات أغنى (شعار، شركة، خوادم مجمّعة بوسيط): استخدمها.**
- إن لم توفّر UI جاهزة: ابنِ الـ modal أعلاه فوق `/api/mt5/brokers` ووسّع الـ proxy ليعيد الشعار + تجميع الخوادم حسب الوسيط إن توفّرت الحقول في استجابة upstream.
- إن لم يتوفر شعار من MetaApi لوسيط معيّن: placeholder محايد + اسم الوسيط — لا تستخدم إيموجي ولا شعارات مزيفة لعلامات تجارية.

## قيود UX

- RTL كامل، عربي أولاً، مع `dir="ltr"` لمعرّفات الحساب/الخادم.
- لا بطاقات زائدة في الـ hero/modal؛ تركيز على التدفق.
- لا تعتمد على انتقال صفحة كامل كالتجربة الوحيدة.
- حافظ على عمل الربط الحالي (`/api/mt/connect`) — غيّر الغلاف البصري والتدفق، لا تعِد اختراع بروتوكول الربط.
- **لا تنفّذ أوامر تداول حية لاختبار الربط.** التحقق بنيوي + اتصال حساب تجريبي إن وُجد فقط.

### معيار القبول للجزء 2

- [ ] أيقونة MT5 الرسمية تفتح modal الربط.
- [ ] بحث وسيط لحظي → اختيار → انتقال تلقائي لشاشة الدخول.
- [ ] شعارات الوسطاء تظهر عند توفرها من المصدر.
- [ ] قائمة خوادم الوسيط المختار ظاهرة قبل/أثناء الدخول.
- [ ] زر إلغاء الربط يعمل، ويمكن ربط حساب جديد بعدها.
- [ ] يعمل على سطح المكتب والجوال.

---

# الجزء 3 — باقة واحدة + تجربة مجانية (ساعة + 3 توصيات)

## المنتج

| الخطة | السعر | الحدود |
|---|---|---|
| تجربة مجانية | 0$ | **كل المميزات** لمدة **60 دقيقة** من لحظة بدء التجربة، وبحد أقصى **3 توصيات** (أيهما ينتهي أولاً يقطع الوصول) |
| الوصول الكامل | **180$** | كل شيء — بدون حد الـ 3 توصيات وبدون نافذة الساعة |

## التنفيذ المطلوب

1. **حدّث `AICHART_PLAN` في `web/src/lib/subscription/plan.ts`:**
   - السعر المعروض للمستخدم: **180$** (الترويجي الحالي موجود؛ وحّد الرسائل حوله).
   - أبقِ `trialInteractions: 3`.
   - أضف صراحةً `trialDurationMs` أو `trialDurationMinutes: 60` وابدأ الساعة من لحظة تفعيل التجربة (أول استخدام حقيقي أو إنشاء صف الـ entitlement — اختر سلوكاً واحداً وثبّته في الكود والتعليقات والاختبارات).

2. **فرض الحد الزمني في `resolveEntitlement` / `trialQuota`:**
   - إن انتهت الساعة أو استُهلكت الـ 3 توصيات → `access: "blocked"` مع رسالة واضحة تدفع للاشتراك بـ 180$.
   - أضف عمود وقت بدء التجربة إن لزم (`trial_started_at`) في **sqlite و pg معاً** (`web/src/lib/db/sqlite.ts` و `web/src/lib/db/pg.ts`).

3. **وحّد صفحة الأسعار والفوترة على باقة واحدة:**
   - أزل أو أوقف عرض أربع باقات `TIERS` من الواجهة العامة إن كانت تتعارض مع باقة Lonora الواحدة.
   - النصوص: باقة واحدة، تجربة ساعة + 3 توصيات، ثم 180$.
   - لا تخترع دورة شهرية/سنوية في الواجهة إن كان التفعيل يدوياً عبر تيليجرام/أدمن كما هو اليوم — حافظ على قناة التفعيل القائمة (`AICHART_PLAN.telegramUrl` / لوحة الأدمن) ما لم يطلب تغيير بوابة الدفع صراحةً.

4. **«توصية» = ماذا بالضبط؟**
   - عرّفها كـ **تفاعل وكيل مكتمل ينتج توصية تداول** (نفس عدّاد `trial_interactions` القائم)، لا كضغطات UI عشوائية.
   - لا تخصم عند `operational_blocker`.
   - حدّث الرسائل العربية/الإنجليزية في `trialQuota.ts` لتشمل انتهاء الساعة وليس فقط نفاد الـ 3.

### معيار القبول للجزء 3

- [ ] مستخدم جديد: تجربة بكل المميزات، تُغلق بعد 60 دقيقة أو بعد 3 توصيات.
- [ ] الرسالة عند الإغلاق تذكر السعر 180$ وقناة التفعيل.
- [ ] صفحة الأسعار تعرض باقة واحدة متسقة مع `AICHART_PLAN` (لا أربع بطاقات متضاربة).
- [ ] اختبارات الوحدة لـ entitlement/trial تغطي: نفاد العدد، انتهاء الساعة، والأدمن غير مقيّد.

---

# ترتيب التنفيذ المقترح

1. الجزء 1 (إزالة OANDA + بوابة MT) — يكسر الافتراضات القديمة مبكراً ويظهر أخطاء TypeScript الحقيقية.
2. الجزء 2 (واجهة الربط) — لأن الجزء 1 يجعل الربط شرطاً؛ يجب أن تكون التجربة ممتازة فوراً.
3. الجزء 3 (الاشتراك/التجربة) — مستقل نسبياً لكن يجب أن يبقى متسقاً مع نصوص CTA بعد الربط.

بعد كل جزء: commit منفصل واضح على نفس الفرع + الاختبارات ذات الصلة خضراء.

---

# حواجز صارمة

- المستودع خاص وفيه مكتبة TradingView مرخّصة — لا تفتح المستودع ولا تسرّب ملفات vendor.
- لا أوامر تداول حية على حسابات حقيقية أثناء الاختبار.
- لا تكسر MCP tools التي تعتمد بيانات السوق؛ عدّل المصدر لا تحذف أدوات التحليل.
- لا تُبقِ مسارات ميتة أو مفاتيح ترجمة يتيمة أو ذكر OANDA كمصدر بيانات حي في docs/UI.
- عدّل `sqlite` و`pg` معاً لأي تغيير مخطط.
- لا تقدّر وقتاً بالأيام؛ نفّذ على مراحل صغيرة قابلة للمراجعة.

---

# ابدأ الآن

1. اسحب أحدث `main`.
2. اقرأ الملفات المذكورة في جدول «ما هو مبني مسبقاً».
3. نفّذ `rg -n "oanda|OANDA|MarketDataSource|AICHART_PLAN|Mt5ConnectWizard|TIERS" web/src` وابنِ قائمة ملفات دقيقة قبل أول commit.
4. ابدأ بالجزء 1، ولا تنتقل للجزء التالي قبل خضرة اختباراته ومعايير قبوله.

عند الانتهاء من كل جزء، أبلغ بما تغيّر، وما بقي، وأي قرار اتخذته حول واجهة/شعارات MetaApi إن اختلفت عن الافتراض أعلاه.
