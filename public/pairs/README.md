# Pair / metal artwork

Vendored from https://github.com/nvstly/icons (`forex_icons/`), imported at the
operator's request. PNG, transparent background. Currently referenced: XAU.png
and XAG.png (real gold/silver marks replacing the CSS tint discs); country
currencies keep the higher-quality local SVGs in `/public/flags`. No CDN — the
navigation test forbids remote icon URLs.
