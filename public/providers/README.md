# AI provider marks

Vendored from https://github.com/lobehub/lobe-icons (MIT), `packages/static-svg/icons/`.
These files are the reference copies; the UI inlines the same paths in
`src/components/agent/ProviderIcon.tsx` so `currentColor` follows the theme.
